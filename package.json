{
  "name": "slideshot-monorepo",
  "private": true,
  "workspaces": [
    "packages/cli",
    "packages/mcp-server"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:cli": "npm run build -w packages/cli",
    "build:mcp": "npm run build -w packages/mcp-server"
  }
}
